# Step-by-Step Setup
Clone the repository: Open your terminal, navigate to your desired directory, and clone the project from your Git provider.


git clone https://github.com/raheel2wali/laravelcoalitiontest.git
cd laravelcoalitiontest


Install dependencies: Install all the necessary PHP dependencies defined in the composer.json file.

composer install
